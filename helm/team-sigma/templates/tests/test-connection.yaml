apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "team-sigma.fullname" . }}-test-connection"
  labels:
    {{- include "team-sigma.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "team-sigma.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
