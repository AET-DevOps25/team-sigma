spring:
  application:
    name: ${SPRING_APPLICATION_NAME:genai-service}

server:
  port: ${SERVER_PORT:8084}

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka/}
